questions on pointer modifications. Example printf (*(p+x)). What would this print? Can x be an unsigned int?

Advantages or uses of static keyword. Give examples

Use of keyword volatile or what happens when volatile is used.

threading and multi processing in C

write  code to sort numbers. Various sort algorithms (use of pointers)

Optimizing the sort code for speed.

What happens if ++i is used and if i++ is used.

Write a code to reverse a character string. (Use of pointers)

round up numbers that are multiple of 64

How to check if a number is divisible by 2? One line code. Code should not use % operator. (Bitwise operations)

Why should global keyword not be used in programming or what is happens if global is used.
